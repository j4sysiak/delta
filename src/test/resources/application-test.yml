spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/delta_test
    username: delta
    password: delta
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true